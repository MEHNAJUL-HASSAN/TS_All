// null means nothing (null); we use null when we don't want to assign any value to a variable.

//difference between null and undefined is null means null is assigned to a variable and undefined means till now value is not assinged to a variable.

let val:null;
console.log(null);